spring:
  application:
    name: gym-crm
  profiles:
    active: dev

  aop:
    proxy-target-class: false

---

spring:
  config:
    activate:
      on-profile: dev

storage:
  data:
    path: classpath:data/init-data.json

---

spring:
  config:
    activate:
      on-profile: prod